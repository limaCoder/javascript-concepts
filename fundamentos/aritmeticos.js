const [a, b, c, d] = [3, 5, 2, 15] // destructuring

// Operações Binárias = Dois operandos
const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2
const potencia = a ** c
console.log(soma, subtracao, multiplicacao, divisao, modulo, potencia)

/* Ordem de Precedência:

()
**
* / %  (se estiverem juntos, a precedência aqui é quem aparecer primeiro na conta)
+ -    (mesma coisa aqui)

*/
